{
  "mcpServers": {
    "n8n-direct": {
      "command": "npm",
      "args": [
        "--prefix",
        "tools/mcp-n8n",
        "run",
        "start"
      ],
      "env": {
        "N8N_BASE_URL": "${N8N_BASE_URL}",
        "N8N_API_KEY": "${N8N_API_KEY}"
      }
    }
  }
}
